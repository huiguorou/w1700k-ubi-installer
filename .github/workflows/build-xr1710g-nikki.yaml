name: XR1710G UBI OpenWrt with Nikki

on:
  workflow_dispatch:

env:
  REMOTE_REPOSITORY: hurrian/openwrt-w1700k
  REMOTE_BRANCH: xr1710g-safe
  TARGET_SUBTARGET: airoha/an7581
  INNER_CONFIG: configs/xr1710g-nikki.config
  OUTER_CONFIG: configs/minimal.config

jobs:
  build:
    name: Build XR1710G OpenWrt with Nikki
    runs-on: ubuntu-24.04

    steps:
      - name: Checkout installer repository
        uses: actions/checkout@v4
        with:
          path: installer-repo

      - name: Install build dependencies
        run: |
          sudo apt update
          sudo apt install -y \
            build-essential clang flex bison g++ gawk \
            gcc-multilib g++-multilib gettext git libncurses-dev libssl-dev \
            python3 python3-dev python3-setuptools python3-venv python3-pyelftools \
            rsync swig unzip zlib1g-dev file wget curl jq time

      - name: Checkout OpenWrt repository
        uses: actions/checkout@v4
        with:
          repository: ${{ env.REMOTE_REPOSITORY }}
          ref: ${{ env.REMOTE_BRANCH }}
          path: openwrt
          fetch-depth: 0

      - name: Fix incompatible Airoha kernel patches
        run: |
          cd openwrt

          echo "===> Disable incompatible Airoha TX/QDMA patches for Linux 6.18.21"

          rm -f target/linux/airoha/patches-6.18/611-net-airoha-Fix-possible-TX-queue-stall-in-airoha_qdma_tx_napi_poll.patch
          rm -f target/linux/airoha/patches-6.18/612-net-airoha-Add-missing-bits-in-airoha_qdma_cleanup_tx_queue.patch

          echo "===> Clean possible stale kernel build state"
          rm -rf build_dir/target-*/linux-airoha_an7581/linux-6.18.21 || true
          rm -rf build_dir/target-*/linux-airoha_an7581 || true
          rm -rf staging_dir/target-aarch64_cortex-a53_musl/stamp/.target_compile || true

          echo "===> Remaining Airoha 6.18 patches:"
          ls -1 target/linux/airoha/patches-6.18 || true

      - name: Add Nikki feed and install feeds
        run: |
          cd openwrt

          echo "===> Fix OpenWrt feed mirrors"
          sed -i \
            -e 's#https://git.openwrt.org/feed/#https://github.com/openwrt/#g' \
            -e 's#https://git.openwrt.org/project/#https://github.com/openwrt/#g' \
            feeds.conf.default

          echo "===> Add Nikki feed"
          grep -q "OpenWrt-nikki" feeds.conf.default || \
            echo "src-git nikki https://github.com/nikkinikki-org/OpenWrt-nikki.git;main" >> feeds.conf.default

          echo "===> Update feeds"
          ./scripts/feeds update -a

          echo "===> Install feeds"
          ./scripts/feeds install -a
          ./scripts/feeds install -p nikki -a

          echo "===> Remove unrelated broken NFS/video feed packages"
          rm -rf package/feeds/packages/nfs-kernel-server || true
          rm -rf feeds/packages/nfs-kernel-server || true

          rm -rf package/feeds/video/sdl2-compat || true
          rm -rf package/feeds/video/sdl3 || true
          rm -rf package/feeds/video/sdl3-doom || true
          rm -rf package/feeds/video/sdl3-image || true
          rm -rf package/feeds/video/sdl3-mixer || true

          rm -rf feeds/video/sdl2-compat || true
          rm -rf feeds/video/sdl3 || true
          rm -rf feeds/video/sdl3-doom || true
          rm -rf feeds/video/sdl3-image || true
          rm -rf feeds/video/sdl3-mixer || true

          echo "===> Fix Nikki Mihomo recursive dependency"
          echo "===> Keep mihomo-meta, remove mihomo-alpha"
          rm -rf package/feeds/nikki/mihomo-alpha || true
          rm -rf feeds/nikki/mihomo-alpha || true

          echo "===> Clean generated Kconfig cache"
          rm -rf tmp

          echo "===> Verify remaining Nikki packages"
          find package/feeds/nikki -maxdepth 2 -type f -name Makefile | sort || true

      - name: Check XR1710G device support
        run: |
          cd openwrt

          echo "===> Search XR1710G device profile"
          grep -R "gemtek_xr1710g" -n target/linux package/boot 2>/dev/null || true

          echo "===> Search XR1710G related files"
          find target/linux -iname "*xr1710g*" -o -iname "*17xx*" -o -iname "*w1700k*" | sort || true

      - name: Inner Build - XR1710G firmware with Nikki
        run: |
          cd openwrt

          echo "===> Load inner build config"
          cp "../installer-repo/${{ env.INNER_CONFIG }}" .config

          echo "===> Inject default LAN IP: 192.168.10.1"
          mkdir -p files/etc/uci-defaults

          cat > files/etc/uci-defaults/99-set-default-lan-ip <<'EOF'
          #!/bin/sh

          # Set default LAN IP to avoid conflict with common 192.168.1.1 networks.
          # This script runs once on first boot, then OpenWrt removes it automatically.

          uci batch <<'UCI'
          set network.lan.ipaddr='192.168.10.1'
          set network.lan.netmask='255.255.255.0'
          set dhcp.lan.start='100'
          set dhcp.lan.limit='150'
          set dhcp.lan.leasetime='12h'
          commit network
          commit dhcp
          UCI

          exit 0
          EOF

          sed -i 's/^[[:space:]]*//' files/etc/uci-defaults/99-set-default-lan-ip
          chmod +x files/etc/uci-defaults/99-set-default-lan-ip

          echo "===> Expand config"
          make defconfig

          echo "===> Check target config"
          grep -E "CONFIG_TARGET_(airoha|DEVICE_airoha_an7581_DEVICE_gemtek_xr1710g)" .config || true

          echo "===> Check Nikki related config"
          grep -E "CONFIG_PACKAGE_(nikki|luci-app-nikki|luci-i18n-nikki|mihomo-meta|mihomo-alpha|kmod-tun|kmod-dummy|kmod-nft-tproxy|kmod-nft-socket|kmod-inet-diag|ip-full|yq|curl|ca-bundle|firewall4)" .config || true

          echo "===> Check LAN IP customization"
          cat files/etc/uci-defaults/99-set-default-lan-ip

          echo "===> Download sources"
          make -j"$(nproc)" download V=s

          echo "===> Build inner firmware with Nikki"
          make -j"$(nproc)" world V=s

          echo "===> Save inner Nikki artifacts before outer minimal build"
          mkdir -p ../inner-artifacts

          cp "bin/targets/${{ env.TARGET_SUBTARGET }}/openwrt-airoha-an7581-gemtek_xr1710g-ubi-initramfs-recovery.itb" \
             "../inner-artifacts/openwrt-airoha-an7581-gemtek_xr1710g-ubi-initramfs-recovery.itb"

          cp "bin/targets/${{ env.TARGET_SUBTARGET }}/openwrt-airoha-an7581-gemtek_xr1710g-ubi-squashfs-sysupgrade.itb" \
             "../inner-artifacts/openwrt-airoha-an7581-gemtek_xr1710g-ubi-squashfs-sysupgrade-with-nikki.itb"

          echo "===> Inner artifacts saved"
          ls -lh ../inner-artifacts

      - name: Outer Build - UBI installer
        run: |
          cd openwrt

          echo "===> Clean inner-only first boot customization before outer installer build"
          rm -rf files/etc/uci-defaults/99-set-default-lan-ip || true

          echo "===> Load outer minimal config"
          cp "../installer-repo/${{ env.OUTER_CONFIG }}" .config
          make defconfig

          echo "===> Prepare installer files"
          mkdir -p files/installer

          cp "../inner-artifacts/openwrt-airoha-an7581-gemtek_xr1710g-ubi-initramfs-recovery.itb" \
             "files/installer/openwrt-airoha-an7581-gemtek_xr1710g-ubi-initramfs-recovery.itb"

          cp "../inner-artifacts/openwrt-airoha-an7581-gemtek_xr1710g-ubi-squashfs-sysupgrade-with-nikki.itb" \
             "files/installer/openwrt-airoha-an7581-gemtek_xr1710g-ubi-squashfs-sysupgrade.itb"

          rsync -a ../installer-repo/files/ files/

          chmod +x files/installer/install.sh || true
          chmod +x files/lib/preinit/00_installer || true

          echo "===> Build outer installer"
          make -j"$(nproc)" V=s

      - name: Prepare artifacts
        run: |
          mkdir -p output

          echo "===> Collect installer initramfs"
          cp "openwrt/bin/targets/${{ env.TARGET_SUBTARGET }}/openwrt-airoha-an7581-gemtek_xr1710g-ubi-initramfs-recovery.itb" \
             "output/openwrt-airoha-an7581-gemtek_xr1710g-ubi-initramfs-installer.itb" || true

          echo "===> Collect chainload uboot"
          cp "openwrt/bin/targets/${{ env.TARGET_SUBTARGET }}/openwrt-airoha-an7581-gemtek_xr1710g-ubi-chainload-uboot.itb" \
             "output/openwrt-airoha-an7581-gemtek_xr1710g-ubi-chainload-uboot.itb" || true

          echo "===> Collect Nikki sysupgrade from inner artifacts"
          cp "inner-artifacts/openwrt-airoha-an7581-gemtek_xr1710g-ubi-squashfs-sysupgrade-with-nikki.itb" \
             "output/openwrt-airoha-an7581-gemtek_xr1710g-ubi-squashfs-sysupgrade-with-nikki.itb" || true

          echo "===> Collect Nikki recovery from inner artifacts"
          cp "inner-artifacts/openwrt-airoha-an7581-gemtek_xr1710g-ubi-initramfs-recovery.itb" \
             "output/openwrt-airoha-an7581-gemtek_xr1710g-ubi-initramfs-recovery-with-nikki.itb" || true

          echo "===> List output"
          find output -maxdepth 1 -type f -print -exec ls -lh {} \;

      - name: Upload artifacts
        uses: actions/upload-artifact@v4
        with:
          name: xr1710g-openwrt-nikki
          path: output/
